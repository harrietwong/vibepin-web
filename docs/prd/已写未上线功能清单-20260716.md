# 已写未上线功能清单（2026-07-16）

> 背景：07-16 生产回退事故恢复后，对快照回灌覆盖的 75 个业务文件做了全量丢失审计。
> 真丢失的 5 处已全部修复上线（undo boardName / hover 展开 / 卡片媒体回退链 / generating 过滤守卫 / 帮助文案）。
> 本文档记录的是**代码已存在但有意未恢复/未启用**的功能——将来要上线时按"恢复路径"操作即可。
> 维护约定：上线其中一项后，把该条移到文末"已上线"区并注明 commit。

---

## 1. Settings → Support 的 "My support tickets" 入口

- **描述**：Settings 弹窗 Support 标签里的 "View my tickets" 按钮（TicketCheck 图标卡片），点击跳 `/app/support/tickets`。
- **代码位置**：`git show 8651f79:web/src/components/settings/SettingsModal.tsx` 第 1133–1141 行（`data-testid="support-my-tickets-button"`），图标 import 在第 5 行。
- **未上线原因**：chat-first 客服重设计弱化传统工单入口；2026-07-16 用户明确决定不恢复。
- **恢复路径**：把上述 SupportCard 块移植回当前 `SettingsModal.tsx` 的 Support 标签（工单页 `/app/support/tickets` 本身仍在线上可用，SupportChat 升级流程也会落到工单）。
- **优先级建议**：低。等工单量大到需要独立入口再说。

## 2. AI 文案的内容语言选择（per-run 语言下拉）

- **描述**：生成 Pin 文案时按次选择输出语言（旧版 Create Pins 有 per-run language select，见 Create Pins PRD v1.2 07-10 验收记录）。
- **现状**：`resolveContentLanguage()`（`web/src/lib/i18n/config.ts`）恒返回 `"en"`，AI 文案只出英文；`draftPreferences.contentLanguage` 字段仍在存储但被忽略；Settings 里的"AI 内容语言"下拉已按 rc0 计划移除（test-i18n 断言此行为）。
- **代码位置**：旧 UI 实现在 board v2 之前的 studio 页面历史里（`git log -- web/src/app/app/studio/page.tsx` 07-10 前后）；Settings 侧旧 UI 在 `git show 8651f79:web/src/components/settings/SettingsModal.tsx` / `LanguageRegionModal.tsx`（`ai.contentLanguage` 等 8 个 i18n key 同在该 commit 的 `en.ts`）。
- **未上线原因**：board v2 重写时未继承；rc0 明确把语言决策收敛为"恒英文 + 将来在 Create Pins 加 per-run 下拉"（`i18n/config.ts` 注释）。
- **恢复路径**：不是简单还原——应按 rc0 注释的方向在 board v2 的生成抽屉（AiVersionDrawer / PinAICopyPanel）加 per-run 下拉，把选值传入 `/api/ai-copy`，并让 `resolveContentLanguage` 读它。属重新实现的产品需求。
- **优先级建议**：中。非英语市场用户明确需要时做。

## 3. ContactSupportModal（表单式联系客服）

- **描述**：旧的工单表单式"联系客服"弹窗。
- **代码位置**：`web/src/components/support/ContactSupportModal.tsx`（在树中，已标 `@deprecated`，可编译）。
- **未上线原因**：被 SupportChat / SupportChatModal（chat-first MVP）取代，入口已全部换绑。
- **恢复路径**：任何入口把 `SupportChatModal` 换回 `ContactSupportModal` 即可。
- **优先级建议**：无（保留仅作回退保险，稳定后可删）。

## 4. HIDDEN_PROMPT_VERSION 埋点（被停会话的 WIP）

- **描述**：在 `generation_kept` / `regenerate_clicked` / `generation_deleted` 等 track() 事件里附带 `versions.promptVersion`（取自 `hiddenPromptBuilder.HIDDEN_PROMPT_VERSION`），用于按 prompt 版本分析生成质量。
- **代码位置**：补丁 `docs/patches/wip-hidden-prompt-version.patch`（改 `StudioBoard.tsx` + `PinBoardCard.tsx` 各 3 处 track 调用）。
- **未上线原因**：属 07-16 事故当晚被停掉的会话的未完成工作，未经验证。
- **恢复路径**：`git apply docs/patches/wip-hidden-prompt-version.patch`——注意两文件事故后已大改（PinCardMedia、hover 移植），补丁可能需手动重定位（3 处 track 调用逐个改即可，5 分钟工作量）。
- **优先级建议**：低-中。做创意智能 Phase D 数据分析前顺手带上。

## 5. 创意智能层 4 组件（rc0 拣选保留件）

- **描述**：rc0 拣选时未进正式提交的 4 个组件：beacon、promote 增强、inferP0Category、creativeSelections（见记忆 "rc0 拣选与 stash 暂存 2026-07-15"）。
- **代码位置**：`stash@{1}`（`On master: rc0-wp1-cherrypick-hold`）+ 各 `rc0-rescue-*` tag。
- **未上线原因**：rc0 会话有意暂存，未完成拣选评审。
- **恢复路径**：⚠️ 该 stash 基于 master 世系，**禁止直接 pop 到当前分支**（会覆盖 tracked 修改，参见 CLAUDE.md git 安全规则）。正确做法：`git stash show -p stash@{1}` 导出补丁 → 按文件评审 → 手工移植。归 rc0 会话主人处置。
- **优先级建议**：由创意智能路线图决定。

## 6. pinReadiness 可选 title/desc/alt 的另一版实现（产品决策搁置件）

- **描述**：`stash@{2}`（`wip: pinReadiness optional title/desc/alt, deferred product decision, see GPT WP0.1 review 2026-07-14`）。注意：当前线上 WP1 契约已经是"只有 image+board 拦截"，此 stash 是当时的另一版草稿，**大概率已被现行实现取代**。
- **恢复路径**：先 `git stash show -p stash@{2}` 对照现行 `pinReadiness.ts`，确认无增量价值后可 drop（drop 前打 rescue tag）。
- **优先级建议**：清理项，非功能项。

## 7. 其他并行会话的进行中工作（非本清单管辖，仅登记）

- `Temp/pinterest-flow-cockpit` 工作区：改动中的 `publish-due route`、`api/pinterest/pins route`、`DraftDetailsDrawer`、`StudioBoard`、`pinDraftStore`（自带 rescue-master 世系）。归该会话主人；如继续，必须先 rebase 到 `bcbffa4` 之后。

---

## 已上线（从上方移入）

（空）
