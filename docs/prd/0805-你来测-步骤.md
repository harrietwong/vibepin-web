# 你来测 —— 多账号功能

## 在哪测

**打开浏览器访问 `http://127.0.0.1:3000`**(服务器我已经起好了,连的是**生产库**)。

- 用你自己的账号正常登录(`zhihuihuang321@gmail.com`)
- 看到的是**真实数据**(85 条 Pin、你已连的 Pinterest 和 Facebook)
- ⚠️ 连的是生产库 → **不要真的点发布**,除非你想真发一条到 Pinterest

> 页面右下角有个 `LOCAL` 角标,确认你在本地环境而不是线上。

**Weekly Plan 现在并进 Create Pins 了**,`/app/plan` 会自动跳到 `/app/studio?view=plan`,这是正常的。

---

## 测试 1 —— Settings 账号卡片(1 分钟)

1. 左侧边栏点**齿轮图标**(Settings)
2. 左边菜单点 **Social accounts**

**✅ 应该看到**
- Pinterest 显示绿色 **Connected**
- 两个按钮:`Disconnect Pinterest` + `Add another Pinterest account`
- Facebook 也显示 Connected(`vibepin.co`)
- Instagram 显示 Not connected

**❌ 有问题就是**
- Pinterest 显示 Not connected(说明迁移后应用读不到)
- 页面报错或一片空白

> 📌 卡片上写的是「Account connected」而不是你的 `@用户名` —— **这是正常的**。
> 旧数据里从来没存过账号名,同步一次就会显示。不用报给我。

---

## 测试 2 —— 单账号不该看到多账号控件(2 分钟)

**这条最重要**:你现在只连了 1 个 Pinterest,所以界面**不该**出现任何"选账号"的东西。

1. 进 **Create Pins**(左侧第一个图标)
2. 点顶部 **Plan** 标签
3. 点右上角 **Filters** 按钮

**✅ 应该看到**:筛选面板里**只有 "CATEGORY" 一个下拉**

**❌ 有问题就是**:出现了 "Pinterest account" 下拉

4. 随便点开一个 Pin 的 **Edit details**

**✅ 应该看到**:没有"选择账号"的下拉
**❌ 有问题就是**:出现了账号选择器

---

## 测试 3 —— ⛔ 跳过,别点 Disconnect

**我原本让你点 Disconnect 看确认框,这是错的,已经改掉。**

我去查了代码:确认框**只在该账号还有排程 Pin 时才弹**
(`SocialAccountsPanel.tsx:1422` 的 `if (scheduledCount > 0)`)。
你这个账号的 `scheduledCount` 是 **0**,所以**点下去会直接断开,没有任何确认**。

断开了就得重新走一遍 Pinterest 授权,没必要。**这一步跳过。**

> 这条要真机验,得先让某个已排程的 Pin 绑定到账号上(发布一次就会绑),
> 属于测试 4 之后的事。

---

## 测试 4 —— 加第二个账号(**需要你有第二个 Pinterest 账号**)

没有第二个账号就跳过,前三个测完就够了。

1. Settings → Social accounts → **Add another Pinterest account**
2. 用**另一个** Pinterest 账号授权

**✅ 应该看到**:两行账号并排,**第一个还在**

**❌ 严重问题**:第一个账号不见了 / 变成了第二个账号的名字

### 4b. 有两个账号之后再看这三处

| 看哪里 | 应该出现 |
|---|---|
| Plan → Filters | 多出 **Pinterest account** 下拉 |
| Pin 卡片 | 出现 `@账号名` 小徽标 |
| 每行账号 | 各自有**独立的 Remove 按钮** |

### 4c. 逐个移除(**最关键的一条**)

1. 只点**其中一个**账号的 Remove

**✅ 应该**:只有那一个消失,**另一个还连着**
**❌ 严重问题**:两个一起没了

---

## 报问题给我怎么写

就三句话:

```
测试几:
我看到:
我以为应该:
```

**不用替我判断是什么原因** —— 你看到什么就写什么,原因我来查。

如果是界面问题,**截个图**最快。

---

## 我已经替你验过的(不用重复测)

- 后端接口返回的连接状态正确(`connected`、5 个权限齐全)
- 你的默认板 `homedecor my love` 在迁移中**没丢**
- 移除接口是按账号隔离的,传错 id 不会误删
- 那 24 条排程的 Pin 确实都还没绑账号(所以测试 3 不会弹排程提示)
