# GLM 执行指令：VibePin 实体类目稳定性第一检查点

你是执行 Worker，不是最终决策者。Codex 负责方案、验收和是否进入下一阶段。

## 必读文件

1. `D:\代码\Pinterest flow\AGENTS.md`
2. `D:\代码\Pinterest flow\docs\coordination\AI_ADVISOR_WORKFLOW.md`
3. `D:\代码\Pinterest flow\docs\superpowers\specs\2026-09-12-vibepin-physical-category-stability-design.md`
4. `D:\代码\Pinterest flow\docs\superpowers\plans\2026-09-12-vibepin-physical-category-stability.md`

完整读取四个文件，不要只读摘要。

## 本次目标

只执行实施计划的 Task 1–Task 5：

- 建立历史 Pin ID 与图片 SHA-256 排除索引。
- 为 Home、Fashion 各采集一轮全新实体参考图。
- 为 Beauty、Garden/Outdoor、Food & Drink、DIY & Crafts、Wedding 各采集三轮全新参考图。
- 严格执行“近 183 天、收藏不少于 100、非历史 Pin、非历史图片、非白底商品图/模板/明显广告或 AI”的门槛。
- 合并去重，每类筛 6–10 张；不足时如实标记，不降低门槛。
- 生成七个只有“要 / 不要”按钮的审核页，默认“要”，并完成真实浏览器点击、刷新和导出 QA。

## 工作区要求

1. 必须先创建独立 Git worktree 和独立分支，例如 `glm/physical-stability-20260912`。如果无法创建隔离 worktree，停止并说明，不能在用户当前 checkout 改代码。
2. 允许读取现有仓库和生产数据库，但所有数据库操作必须只读。
3. 不得 push、merge、deploy、删除历史文件、修改生产表、批量写库或改现有功能。
4. 代码和测试可以提交到 Worker 分支；运行时采集证据放在 `output/physical-stability-20260912/`，不要提交大量图片。
5. 不得覆盖现有候选、审核结果或生成产物。

## 模型边界

1. 本检查点不需要模型参与采集；Pinterest 查询、门槛、下载、哈希和审核页均为确定性执行。
2. 不调用 MixToken。
3. 不调用 Fable。
4. 不进行 Gemini 标注或图片生成。
5. 不得把 GLM 的视觉判断写成 Gemini 或 Fable 审查。

## 实施规则

逐项执行计划 Task 1–Task 5，遵循测试先行：先写失败测试，确认失败，再做最小实现，复测通过。不要把多个任务合并成一个大脚本，也不要跳过计划里的清单字段。

遇到以下情况必须停止该类目并保留证据：

- 找不到至少 6 张符合门槛的新参考图。
- Pinterest 返回字段缺失或访问失败，无法证明创建时间和收藏数。
- 图片下载后损坏、哈希不一致或与历史图片重复。
- 审核页按钮、刷新持久化或导出反馈失败。

不得通过降低收藏数、放宽到半年以前、复用旧图或人工补造数据来通过。

## 最终返回格式

完成后只返回以下内容：

1. 独立 worktree 的绝对路径和分支名。
2. Task 1–Task 5 每个提交的 commit hash 和变更文件。
3. 执行过的完整测试命令及 PASS/FAIL 摘要。
4. `reference-run-summary.json` 的绝对路径。
5. 七个 shortlist JSON 的绝对路径。
6. 七个审核 HTML 的绝对路径。
7. `review-page-qa.json` 的绝对路径。
8. 每类原始数量、合格数量、重复率、元数据覆盖率和状态。
9. 所有失败、供应不足和未完成项。
10. 明确声明：是否写入生产数据库、是否调用模型、是否 push/merge/deploy。

完成 Task 5 后停止，不要继续 Task 6 或 Task 7。等待 Codex 独立验收和用户审核。
