# Codex 接管收款与发布：统一候选交接单（2026-08-30）

## 接管范围

- 收款、套餐、用量计量与退款语义：接管自 `integrate/usage-phase1-0829@af7eed8f2028a9eb5d32bd760c02339428fa44e8`。
- 多渠道发布最终修复：接管自 `codex/multichannel-handoff-0830@f1beaa877a2367cec1c0d4f7eb164b805ded8e97`，其父提交为 Claude 的 `integrate/multichannel-0827@e7a26eba`。
- 中央发布基线：接管自 `codex/product-v37-central-integrate-0829@5fe929355c0531f47934aec84248898741be2f6a`。
- 统一接管分支：`codex/payment-publish-handoff-0830`。

## Claude 会话审计

- 收款和定价会话：`061c8163-c84b-4f99-9d94-4a7d9e46f3d1`。最后完成并通过独立复核的候选为 `af7eed8f`；测试数据库已验证 v68，生产数据库未迁移。
- 发布和部署会话：`8686de2d-9f48-4e76-8950-4c0f0739fa98`。最后结论为“可开始分阶段执行”，不是“可直接上生产”；未完成的均为外部环境门禁。

## 合并裁决

- 保留中央候选的 CAS 抢占、逐目的地持久化、失败状态持久化和多账号解析。
- 保留用量候选的共享计量键、签名 bucket relay、四态投递分类、fresh-only release 与 enforce 开关。
- 未连接或账号选择不明确均记为 `preNetwork`，只有 `not_sent` / `rejected` 可退款；`sent` / `unknown` 保持扣量。
- 多渠道最终修复保留服务端 tombstone 与较新本地编辑之间的 outbox 重试，避免同步更新丢失。

## 已完成验证

- TypeScript：通过。
- 合并冲突涉及 ESLint：0 error；测试桩保留 5 个未使用参数 warning。
- 用量 Phase 1 的 22 组门禁：通过。
- Creem webhook ordering：24/24。
- cron refund mapping：31/31。
- publish refund mapping：61/61。
- social-only metering：17/17。
- scheduled-post metering：35/35。
- pin draft sync：29/29。
- schedule social guard：82/82。
- 测试注册表：219 tracked / 211 run / 8 excluded。

## 发布状态与限制

- 本候选尚未推送、未部署、未操作生产数据库或生产开关。
- 旧集成发布清单 `product_opportunities_v37_integrated_release_manifest_60a540f1.json` 绑定旧分支、旧 runtime 和旧多渠道来源，不能作为本统一候选的放行凭证。
- 在进入 Preview 前，需要重新生成并独立校验统一清单，冻结本分支 HEAD、来源提交、产物摘要、迁移顺序和回滚边界。
- 未来迁移顺序应在原中央顺序 `v63 -> v66 -> v67` 后纳入 `v68`；执行前仍需验证 v68 的明确回滚/恢复程序。
- Preview 后仍需完成真实产品页与支付配置核验、浏览器 QA、预部署门禁和 shadow 数据复核，之后才可讨论 enforce 或生产推广。

## 待产品确认

- Pricing #13：Free 套餐文案是否采用 Product 管理为 `—`，Calendar 为“Limited — 可手动安排日历排期，暂不含智能排程建议”。
- S4：额外社媒账号是否作为付费加购项，以及最终价格和权益边界。

