# Minimal deps for VibePin cloud worker (Linux VPS / cron)
httpx>=0.27
python-dotenv>=1.0
curl_cffi>=0.7
cryptography>=42.0
playwright>=1.40
# Process-tree inspection used by preflight_product_supply.py (active-worker /
# Playwright detection) and the hardened run_bootstrap_product_supply.py timeout
# kill verification. Without it both degrade to weaker fallbacks on the VPS.
psutil>=5.9
